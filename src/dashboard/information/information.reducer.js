const initialState = {
  id: null,
  email: '',
  firstname: '',
  name: '',
  phone: '',
  deliveryAddress: null,
  billingAddress: null,
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
